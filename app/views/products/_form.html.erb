<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="d-flex space-between">
    <div class="custom-field">
      <%= form.label "Producto", class:"custom-label" %>
      <%= form.text_field :title, required: true, placeholder: "Nombre del producto" %>
    </div>
    
    <div class="custom-field ml-10">
      <%= form.label "Precio", class:"custom-label" %>
      <%= form.text_field :price, required: true, placeholder: "$ 0.00" %>
    </div>
  </div>

  <div class="field">
    <%= form.label "Descripción", class:"custom-label" %>
    <%= form.text_area :description, required: true %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar", class: "btn btn-main" %>
  </div>
<% end %>
